# @axia-js/x-textencoder

A cross-environment TextEncoder.

Install it via `yarn add @axia-js/x-textencoder`

```js
import { TextEncoder } from '@axia-js/x-textencoder';

...
const encoder = new TextEncoder();
```
